name: amazon-review-inference-env
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - numpy
      - pandas
      - scikit-learn
      - scipy
      - pyarrow
      - joblib
      - azureml-defaults
