name: amazon-review-training-env
channels:
  - conda-forge
dependencies:
  - python=3.9
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - scipy
      - pyarrow
      - joblib
      - mlflow
      - azureml-mlflow
